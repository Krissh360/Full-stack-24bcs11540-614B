# Copy this to .env and replace with your real API key
VITE_EXCHANGE_API_KEY=your_api_key_here

# With Vite, env variables must start with VITE_ to be exposed to the client.
# After creating .env, restart the dev server.
